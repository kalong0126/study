# better-sqlite3 v13 改用 N-API，npm 包里自带全平台预编译二进制
# （node_modules/better-sqlite3/prebuilds/win32-x64.node 等），运行时直接加载，
# 安装期不需要任何编译。
# 但包里带着 binding.gyp，npm 会自作主张跑 node-gyp rebuild —— 机器上没有
# VC++ 工具集就整个安装失败（Windows 常见，Linux 上则白装 python3/make/g++）。
# 所以这里关掉所有依赖的生命周期脚本；本依赖树没有需要 install/postinstall
# 才能工作的包（esbuild 的二进制走 @esbuild/win32-x64 可选依赖，不靠脚本）。
ignore-scripts=true
